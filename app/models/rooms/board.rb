# Rooms whose top level is work threads ("posts") instead of chat, rendered
# as a filterable list or grouped by status. Membership works like
# Rooms::Closed, with explicit members chosen by an administrator or the
# creator. See docs/boards.md.
class Rooms::Board < Room
end
