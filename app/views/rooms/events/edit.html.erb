<% @page_title = "Edit #{@event.title}" %>

<% content_for :nav do %>
  <div class="flex-item-justify-start">
    <%= link_back_to room_event_path(@room, @event) %>
  </div>
<% end %>

<section class="panel">
  <h1>Edit <%= @event.title %></h1>
  <%= render "rooms/events/form", room: @room, event: @event %>
</section>
